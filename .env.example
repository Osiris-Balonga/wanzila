# Optionnel : adresse interne de JSON Server si les deux processus sont hébergés séparément.
# Sur Render, le lanceur démarre JSON Server localement ; aucune variable n'est nécessaire.
JSON_SERVER_URL=http://127.0.0.1:3101
